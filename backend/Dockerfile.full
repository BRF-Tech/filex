# filex full image (~250 MB) — slim base + ffmpeg, ghostscript, libreoffice for full thumbnail support
FROM brftech/filex:slim
USER root
RUN apk --no-cache add \
    ffmpeg \
    vips-tools \
    ghostscript \
    libreoffice \
    poppler-utils \
    fonts-liberation \
    fonts-dejavu
