package db

// (intentionally thin — registry implementation lives in driver.go to keep
// the cohesive set together. This file exists so the public package layout
// matches the auth/ and storage/ packages for consistency.)
