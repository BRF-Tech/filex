{
  "name": "@brftech/filex-desktop",
  "private": true,
  "version": "0.13.1",
  "description": "filex desktop — the filex explorer in a window, with browser sign-in, background folder sync and secure token storage",
  "author": "BRF Tech <security@brf.sh>",
  "license": "MIT",
  "homepage": "https://filex.sh",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "sync-web": "node scripts/sync-web.mjs",
    "fetch-cli": "node scripts/fetch-cli.mjs",
    "build:main": "tsc -p tsconfig.json",
    "build": "pnpm run sync-web && pnpm run build:main",
    "dev": "pnpm run build && electron .",
    "smoke": "electron scripts/plumbing-smoke.mjs",
    "dist": "pnpm run build && pnpm run fetch-cli && electron-builder",
    "dist:linux": "pnpm run build && node scripts/fetch-cli.mjs --platform linux && electron-builder --linux",
    "dist:win": "pnpm run build && node scripts/fetch-cli.mjs --platform win32 && electron-builder --win",
    "dist:mac": "pnpm run build && node scripts/fetch-cli.mjs --platform darwin && electron-builder --mac",
    "clean": "rimraf dist app",
    "e2e:ui": "node scripts/ui-login-e2e.mjs",
    "icon": "electron scripts/make-icon.mjs"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
