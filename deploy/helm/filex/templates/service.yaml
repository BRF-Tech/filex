apiVersion: v1
kind: Service
metadata:
  name: {{ include "filex.fullname" . }}
  labels:
    {{- include "filex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "filex.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
