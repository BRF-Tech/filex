apiVersion: v2
name: filex
description: Self-hosted file manager — Go backend + Vue/React/Web Component frontend, pluggable storage/auth/DB drivers, end-to-end encrypted folders, reachable over HTTP, S3, SFTP, FTPS, NFS and WebDAV.
type: application
version: 0.2.14
# Image tags on ghcr.io carry the "v" prefix (ghcr.io/brf-tech/filex:vX.Y.Z) —
# keep it here so the default image ref (filex.image helper) resolves.
# appVersion is kept in step with the product by scripts/sync-chart-version.mjs.
appVersion: "v0.34.2"
home: https://github.com/BRF-Tech/filex
sources:
  - https://github.com/BRF-Tech/filex
keywords:
  - file-manager
  - self-hosted
  - s3
  - webdav
  - sftp
  - onlyoffice
maintainers:
  - name: BRF-Tech
