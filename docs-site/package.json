{
  "name": "@brftech/filex-docs",
  "private": true,
  "version": "0.30.1",
  "type": "module",
  "description": "docs.filex.sh — VitePress documentation site (sources live in ../docs)",
  "scripts": {
    "releases": "node scripts/fetch-releases.mjs",
    "dev": "npm run releases && vitepress dev",
    "build": "npm run releases && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
