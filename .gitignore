# --- Build outputs ---
/bin/
/goreleaser-dist/
/backend/dist/
/backend/embed/web/
/backend/embed/admin/
/packages/*/dist/
/web/dist/
/web/dev-dist/
/desktop/dist/
/desktop/app/
/desktop/release/
# The bundled sync engine, produced at package time by scripts/fetch-cli.mjs.
# ⚠ ~150 MB, and on Linux it has no extension — the `*.exe` rule below does not
# catch it, so a plain `git add` would push the blob into history.
/desktop/build/bin/
/demo/dist/
/docs-site/.vitepress/dist/
/docs-site/.vitepress/cache/
/docs-site-dist.zip

# --- Go ---
*.exe
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
coverage.html
go.work
go.work.sum

# --- Node ---
node_modules/
.pnpm-store/
.turbo/
.cache/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn/

# --- IDE ---
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# --- Env ---
.env
.env.local
.env.*.local
.env.production
config.local.yaml

# --- Local dev DB / data ---
*.sqlite
*.sqlite3
*.sqlite-journal
*.db
*.db-shm
*.db-wal
/data/
/storage-data/

# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# Windows reserved name — bash on Windows occasionally creates a file
# called "NUL" when scripts redirect to /dev/null via mistyped paths;
# git-on-Windows refuses to delete it cleanly so we just ignore it.
NUL

# --- Cypress artefacts ---
/web/cypress/videos/
/web/cypress/screenshots/
/web/cypress/downloads/

# --- Misc ---
*.pid
*.seed
*.tar.gz
*.tgz
.pnp.*
.rid

# Locally built test binary (see desktop e2e: a server carrying unreleased
# endpoints has to be run before the desktop flow can be exercised).
/backend/filex-test
/backend/filex-test.exe
